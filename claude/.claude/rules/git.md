# Git Workflow

- CRITICAL: NEVER USE --no-verify WHEN COMMITTING CODE. No exceptions.
- NEVER stage (`git add`) or commit code unless Chebu explicitly asks you to. Chebu reviews and
  commits manually.
